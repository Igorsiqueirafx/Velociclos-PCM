// This file is replaced by app/sitemap.ts (API route) and app/site-map/page.tsx (visual sitemap)
// Redirect to the visual sitemap page
import { notFound } from 'next/navigation'

export default function () {
  notFound()
}
